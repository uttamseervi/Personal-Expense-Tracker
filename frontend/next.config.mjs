/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com','images.unsplash.com','images.remotePatterns.com'],
    },
};

export default nextConfig;
